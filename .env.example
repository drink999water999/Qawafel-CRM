# ================================================================
# REQUIRED - Must be set for the app to work
# ================================================================

# Your database connection URL
# PostgreSQL example: postgresql://username:password@localhost:5432/qawafel_crm
# MySQL example: mysql://username:password@localhost:3306/qawafel_crm
DATABASE_URL="postgresql://postgres:password@localhost:5432/qawafel_crm"

# Your application URL (keep as-is for development)
NEXTAUTH_URL="http://localhost:3000"

# Secret key for NextAuth (can be any random string for development)
# For production, generate with: openssl rand -base64 32
NEXTAUTH_SECRET="my-super-secret-key-change-this-in-production"

# ================================================================
# OPTIONAL - Only needed if you want Google Sign-In
# Leave empty to disable Google Sign-In (email/password still works!)
# ================================================================

# Get these from: https://console.cloud.google.com/
# Instructions: See GOOGLE_OAUTH_SETUP.md
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ================================================================
# QUICK SETUP INSTRUCTIONS
# ================================================================
#
# 1. Copy this file:
#    cp .env.example .env.local
#
# 2. Edit .env.local:
#    - Set DATABASE_URL to your database connection
#    - NEXTAUTH_SECRET can be any random string
#    - Leave Google fields empty (unless you want Google login)
#
# 3. Setup database:
#    npx prisma db push
#    npx prisma db seed
#
# 4. Verify setup:
#    npm run test:db
#
# 5. Start app:
#    npm run dev
#
# 6. Login at http://localhost:3000/login with:
#    Email: mohamed.hussein@qawafel.sa
#    Password: admin
#
# ================================================================
# NOTES
# ================================================================
#
# - Email/password login works WITHOUT Google OAuth
# - Google OAuth is completely optional
# - If Google credentials are empty/missing, Google button won't work
#   (but that's OK - just use email/password)
#
